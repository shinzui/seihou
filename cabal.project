packages:
  seihou-core
  seihou-cli
  seihou-okf-extension

write-ghc-environment-files: never
-- Keep `cabal build all` and `cabal test all` on a single install plan.
-- Without this the solver picks a plan with no test components and
-- `cabal test all` fails with [Cabal-7043].
tests: True
